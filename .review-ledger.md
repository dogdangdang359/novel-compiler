# Review 台账 — novel-compiler

> incremental-code-review 工作流维护,每轮 review 结束必须更新。

## 版本轨迹

| 轮次 | 日期 | 对象版本 | 结论 |
|---|---|---|---|
| 1 | 2026-08-29 | novel-compiler-0.4.4(仅此一版,无前序可对比) | 修复版,可发布;测试 649/649 全绿 |
| 2 | 2026-08-29 | novel-compiler-0.4.4(同版追加修复) | O-1/O-2/O-3 全部修复;测试 654/654 全绿 |
| 3 | 2026-08-29 | novel-compiler-0.4.4(审查,无修复) | 全量复审 server/simulate/rebase/src/CLI/测试;基线 654/654 全绿,新开 O-4~O-9 |
| 4 | 2026-08-29 | novel-compiler-0.4.4(同版追加修复) | O-4/O-5 已修复;测试 660/660 全绿 |
| 5 | 2026-08-29 | novel-compiler-0.4.4(同版追加修复) | O-6 已修复;测试 669/669 全绿 |
| 6 | 2026-08-29 | novel-compiler-0.4.4(同版追加修复) | O-8 已修复;测试 673/673 全绿 |
| 7 | 2026-08-29 | novel-compiler-0.4.4(同版追加处理) | O-9 已处理(注释说明,无行为变更);测试仍 673/673 |
| 8 | 2026-08-29 | novel-compiler-0.4.4(同版追加整理) | 规则注册表重构完成;测试 736/736 全绿 |
| 9 | 2026-08-29 | novel-compiler-0.4.4(同版追加) | 规则注册表接入 wjrc 元数据;测试 750/750 全绿 |
| 10 | 2026-08-29 | novel-compiler-0.4.4(同版追加功能) | 多 LLM 供应商适配层 + 世界观图谱可视化;测试 776→813 全绿(该轮未登记台账,补记) |
| 11 | 2026-08-29 | novel-compiler-0.4.4(同版追加功能) | 剧情健康度雷达舱(本应本轮审查,实际补记于第 12 轮);测试 813→833 全绿 |
| 12 | 2026-08-29 | novel-compiler-0.4.4(同版追加功能) | review 健康度雷达舱增量;测试 833/833 全绿;新开 O-10-1~O-10-5(见下) |
| 13 | 2026-08-29 | novel-compiler-0.4.4(同版追加修复) | 修复 O-10-1(评分代偿矛盾)+ 锁定测试;测试 833→837 全绿 |
| 14 | 2026-08-29 | novel-compiler-0.4.4(同版追加修复) | 修复 O-10-2~O-10-5(口径/权重/跨面板/注释);测试 837→845 全绿 |
| 15 | 2026-08-29 | novel-compiler-0.4.4(同版配套文档) | README 补 /api/health、/api/worldgraph、健康度/世界图谱面板功能条目与 0.4.4 changelog;测试 845/845 全绿 |

## 修复验证记录(第 13 轮)

> R13-O-10(第 12 轮开出,优先级最高):health.js 在 total=0(无悬念)时用 knowledge 量代偿 resolve 与 inFlight 维度,导致"无悬念但设定丰富"的世界评分虚高到 100「优秀」,与 issues 的 no-suspense 告警自相矛盾;且该代偿分支场景 64 未覆盖。

| ID | 问题 | 修复位置 | 验证结果 |
|---|---|---|---|
| O-10-1 | resolveRate(:61) 与 inFlight 维度(:97) 的 `total=0` 兜底代偿 | src/health.js:60-61 resolveRate 改为 `total ? resolved/total : 0`;:92-97 注释承诺"结构健康评分零悬念即单薄,绝不因世界观量对冲",inFlight 改 `total ? ... : 0`,resolve 沿用 0 | ✅ 修复前"5 实体+6 知识+0 悬念"=100 优秀/告警 no-suspense;修复后同输入 resolve=0、inFlight=0、world=1、评分 55 需打磨,告警保留不再矛盾。场景 64 ④ 新增 4 断言锁定:resolve/inFlight 归 0(核心不代偿)/world 不被牺牲/评分 55 需打磨(防虚高回归)/仍报 no-suspense。README 同步 837。 |

## 修复验证记录(第 14 轮)

> R14-O-10-2~5(第 12 轮开出,🟡 集):健康度雷达舱的维度口径与可观察性问题。逐条修复并用锁定断言锚定。

| ID | 问题 | 修复位置 | 验证结果 |
|---|---|---|---|
| O-10-2 | cast 维度用 `entities.length/5` 统计全部实体(含地点/道具),标签「角色聚集」名不副实 | src/health.js: cast 改按 `entities.filter(type==='character').length` 计;头注释更新「location/item 不计入」 | ✅ 问题样本 cast 由 0.6→0.4(2角色/5);新增场景 64 ⑤ 断言:零角色多地点→cast=0,问题样本 cast=0.4 |
| O-10-3 | 综合评分六档权重内联魔法数字,无注释判据 | health.js 顶部提取 `HEALTH_WEIGHTS` 常量(附风险维加权理由),评分抽为 `computeScore(dimensions, weights)` 导出供测试锚定 | ✅ 新增断言:权重六键且和=1 / 全维满帆→100 / 全维归零→0;调权只需改常量 |
| O-10-4 | worldgraph.openSuspense 与 health 的 overdue+dangling 口径不一,两面板对不上账 | health.js suspense 增加 `open = overdue+dangling` 字段,注释声明与 worldgraph.openSuspense 对齐 | ✅ 交叉断言(同一原始 canon 双端推导):示例 open=0=openSuspense,问题样本 open=2=openSuspense |
| O-10-5 | avgResolutionLag 口径 `+1` 偏移(同章回收显示 1);activeEntities 无退场未说明 | health.js avg 改 `resolved-planted`(同章=0,注释注明 schema 保证 resolved>=planted);activeEntities 补近似注释(canon 无退场字段,出现即持续在役) | ✅ 示例 avgLag 2→1,新增同章回收→章差 0 断言;既有断言同步更新 |

## 修复验证记录(第 8 轮)

> R8-RR(规则注册表):review-report 多轮遗留「rules.runChecks 9 段规则拆成独立函数 + 注册表(单函数 200+ 行、单元粒度稍粗)」。本轮将 src/rules.js 重构为规则注册表,行为零变化(所有既有测试通过),单元粒度提升,补注册表专项单测。

| ID | 问题 | 处理 | 结论 |
|---|---|---|---|
| R8-RR | rules.runChecks 单函数 200+ 行,9 段规则不可插拔、单元粒度粗 | src/rules.js 重构:每条规则即独立对象 `{ id, layer, name, emits, run(ctx) }`,共 10 条(含 5b 删章幽灵)依序注册进 RULES 注册表;runChecks 保持 `{ canon, parsed, mentions, quotes, cfg, file }` 签名不变,一次性派生共享 ctx(chapters/lastChapterNumber/headingLineOf/timeline/chapterNumbers)后依序 `rule.run(ctx)` | ✅ 行为零变化:重构后首跑 729/729 全绿(先于补测);场景 62 新增 7 断言:注册表 10 条完整/顺序稳定/每条含 id+layer+name+emits+run 且 emits 含主 id/demo 产出全部 rule id 均在 emits 声明内(demo 覆盖 ≥5 类)/层归属 L0·L1·L3 无漂移/每条 run 可脱离 runChecks 直接调用(空结构 ctx 不抛) |

## 修复验证记录(第 9 轮)

> R9-WJRC(规则接入 wjrc 元数据):续 R8-RR 的扩展点。规则注册表成为 .wjrc.json 的元数据源,未知规则 id / 未知阈值键警告并忽略,杜绝配置拼错后静默失效。

| ID | 问题 | 处理 | 结论 |
|---|---|---|---|
| R9-WJRC | .wjrc.json 的 rules 键与 thresholds 键是自由字符串,规则 id 拼错/阈值键拼错会静默无效,未接入规则注册表元数据 | src/rules.js 导出元数据视图 `LEGAL_RULE_IDS`(emits 并集)、`RULE_NAMES`(id→名)、`RULE_LAYERS`(id→层级);src/wjrc.js:`applyWjrc` 校验 rules 键必须在 LEGAL_RULE_IDS 内、thresholds 键必须在白名单内,未知项警告并忽略,返回 `invalidRules`/`invalidThresholds`;compile.js manifest 增加 `wjrcInvalid` 字段暴露未知项 | ✅ 行为:合法配置不变(既有测试 736 保留通过);场景 63 新增 14 断言:元数据视图完整性(去重/覆盖/名/层级)/未知规则 id 进 invalidRules 且不进 overrides/未知阈值键进 invalidThresholds 且不覆盖 cfg/合法配置 invalid 为空/无 wjrc invalid 为空/e2e manifest.wjrc 命中+manifest.wjrcInvalid 暴露两项+未知规则未实际覆盖。README 补充 rules/thresholds 校验行为说明 |
| R9-WJRC-B | 上轮补齐的场景 63 覆盖常规路径,但 rule value 形态、未知 id 分类分级、阈值键类型边界不足 | 新增场景 64 共 26 断言(stub warn 隔离噪音):① 合法 id 的 value 形态判定(off/false/{enabled:false}/severity 原样/{enabled:false,severity} off 优先/{enabled:true}不覆盖/空对象/非法值不覆盖 + 均不触发 warn);③ 多个未知 id 全进 invalidRules、按 key 一次 warn 且警告含 id、大小写敏感('Entity/missing' 视为未知);④ 阈值键边界(字符串数字/布尔回退默认、未知键含空串列出、thresholds 不触发 warn、thresholds 为字符串/数字不崩溃);⑤ rules 为字符串/数组不崩溃;⑥ 每次调用 invalid 数组独立(无状态泄漏);⑦ applyRuleOverrides 应用层(空 overrides 原引用/改写同级别不新建对象/off 全删+未覆盖保留) | ✅ 776/776 全绿;README 同步 776 |

## 规则注册表(结构性观察)

- 新增规则只需在 src/rules.js 的 RULES 末尾追加对象,无需改 runChecks 主体——规则不可插拔遗留已消解。
- `emits` 字段声明该规则实际产出的 problem rule id 全集,场景 62 ③ 校验「实际产出 ⊆ emits 声明」,新增规则忘更新 emits 会立刻被场景 62 拦下。
- 顺序即问题输出顺序,注册表条目顺序勿乱排(场景 62 ① 用硬编码顺序锚定)。
- 注册表元数据(`LEGAL_RULE_IDS`/`RULE_NAMES`/`RULE_LAYERS`)已接入 .wjrc.json 校验——写错规则 id 或阈值键会警告并忽略而非静默,manifest.wjrcInvalid 可供 IDE/用户排查。

## 修复验证记录(第 7 轮)

> O-9 为文档性处理,无行为变更,无新增断言。

| ID | 问题 | 处理 | 结论 |
|---|---|---|---|
| R7-O-9 | extract-llm 分块 checkpoint(<out>.partial)未列入 lockTargets | extract-llm.js:482-485 checkpoint 写入处注释说明「不纳入锁目标的理由」 | 复核确认检查点**已被 outFile 写锁间接互斥**:写锁在入口 128-141 行先于一切输入读取(含 checkpoint 读取 149 行)获取,写 checkpoint(486 行 if(outFile))只发生在持有 outFile 锁的进程内,outFile 锁互斥即串行化 .partial 读写,不存在"无锁写 .partial"路径。选择台账建议的「文档注明不要求独占」而非加独立锁——加锁反而会在崩溃残留陈旧 .partial.lock 时于 30min 内挡住自己的 --resume(与 outFile 锁不同,.partial 非共享输出协议成员)。并发写冲突回归由场景 52 ⑤(run-tests.js:3792-3798,--canon-out/--out 同走 acquireFileLocks)覆盖 |

## 修复验证记录(第 6 轮)

> 行号以第 6 轮修复后代码为准;场景 49c-6 新增文本预算升级回归(4 断言)。

| ID | 问题 | 修复位置 | 验证结果 |
|---|---|---|---|
| R6-O-8 | llm.js chatTextValidated 无 finish=length 预算升级(chatJSON 140-142 有)——推理模型把预算花在 reasoning 上致 content 空/截断,旧实现永不升级,三次校验失败后放弃且把空内容当"上次输出"喂回污染上下文 | src/llm.js:196-215 引入 budget 变量,finish=length 且 budget<MAX_BUDGET 时翻倍重试(不喂校验反馈),与 chatJSON 对称 | ✅ TDD:RED 4 项按缺陷失败(msgLens=[1,3,5] 实锤不升级+污染),GREEN 后 4 项全过,673/673 |

## 修复验证记录(第 5 轮)

> 行号以第 5 轮修复后代码为准;场景 57 新增读协调回归(9 断言)。

| ID | 问题 | 修复位置 | 验证结果 |
|---|---|---|---|
| R5-O-6 | simulate.js/rebase.js 输入读取无 checkFileLocked 读协调(compile/compare 第 1 轮已有)——simulate 全量长计算、rebase 漂移判定均基于写入前快照 | simulate.js:94-100 读取 outline/premise/canon 前检查;rebase.js:75-81 读取 pred/actual 前检查——命中进 stderr 非致命警告(与 compile.js:60-63 口径一致) | ✅ TDD:RED 4 项新警告断言按缺陷失败(无检查),GREEN 后 9 项全过,669/669 |

## 修复验证记录(第 4 轮)

> 行号以第 4 轮修复后代码为准;场景 56 内新增 ⑨ 写锁回归(5 断言),场景 52 恒真断言改真(净 +1)。

| ID | 问题 | 修复位置 | 验证结果 |
|---|---|---|---|
| R4-O-4 | POST /api/risks 写 risk-status.json 无写锁——读-改-写竞态,并发 POST 后写覆盖先写、静默丢失状态更新;GET 已有 checkFileLocked 读协调,写侧不对称 | app/server.js:662-707 acquireFileLockAsync(statusFile) 覆盖 reset 与状态更新两分支,冲突 409(与 apply-review/run-stream 口径一致),send helper 先释放锁再回包 | ✅ TDD:RED 3 项新断言按缺陷失败(got 200、状态文件被并发覆盖为 handled),GREEN 后 5 项全过,660/660 |
| R4-O-5 | run-tests.js:3552 恒真伪断言 `check('释放后可重新获取（锁可复用）', true, '')`——不验证任何行为 | run-tests.js:3550-3553 改为 2 个真实断言:二次获取后锁文件重现、二次释放后无残留 | ✅ 锁可复用行为被真实断言覆盖(不再依赖"未抛错即通过") |

## 修复验证记录(第 1 轮)

| ID | 问题 | 修复位置 | 验证结果 |
|---|---|---|---|
| R1-#3 | LOCK_STALE_MS 硬编码 30min 与可配 TOOL_TIMEOUT_MS 脱钩,调高超时后合法长运行锁被误判陈旧回收 | src/fsutil.js:88 `Math.max(30*60*1000, Math.ceil(TOOL_TIMEOUT_MS*1.5))` | ✅ 派生式实现,含 NaN/负值回退(fsutil.js:42-43);3 组派生单测 + 容错单测通过(run-tests.js:3566-3595) |
| R1-#4 | 书名对齐 TOCTOU:无锁预读→拿锁→写,窗口内以陈旧快照覆盖并发写者 | refactor.js:285-311 锁内重读 fresh 重判定 | ✅ 读改写全周期在锁内;场景 49l Part A 5 断言(含"对齐跳过"并发分支未破坏) |
| R1-#5 | 回滚路径未持锁且非原子(copyFileSync 在子进程退出后执行,与并发写者竞争) | refactor.js:153-170 rollbackFile + heldLockTargets(146) | ✅ 已持锁直接原子恢复;canon 临时取锁+原子替换;锁冲突跳过回滚保留备份。场景 49l Part B 6 断言 |

## 已清零(🔴 级)

- 无遗留 🔴 级问题。第 1 轮三个待修问题(#3/#4/#5)全部验证通过。

## 修复验证记录(第 2 轮)

> 行号以第 2 轮修复后代码为准;第 1 轮记录中的行号对应修复前版本。回归场景 49m 插入于 49l 之前,其后所有测试行号 +85。

| ID | 问题 | 修复位置 | 验证结果 |
|---|---|---|---|
| R2-O-1 | ② 大纲落盘 atomicWrite 无 try/catch——失败传播到 main().catch exit 2,不回滚 canon,残留"新 canon + 旧 outline/premise"不一致 | refactor.js:240-249 try/catch:提示"大纲写入失败"→ rollbackFile(b1, canonPath, 'canon') → exit 1,与 ③ premise 回滚对称 | ✅ TDD:RED 阶段 4 项新断言失败复现缺陷(got 2/无回滚提示/canon 残留新书名),GREEN 后全过 |
| R2-O-2 | ② 大纲落盘失败路径无回归测试(此前全测试仅 ③ premise 一处 no-such-dir) | run-tests.js:2674-2760 场景 49m:大纲写至 no-such-dir 必败,fake 服务器 ①② 正常、③ 不可达 | ✅ 5 断言:exit 1/提示大纲写入失败/提示已回滚 canon/canon 恢复原书名/premise 未动 |
| R2-O-3 | 场景 52 本地 `LOCK_STALE_MS = 30*60*1000` 构造安全性依赖"派生下限恰为 30min",无注释 | run-tests.js:3534-3535 常量上方 2 行注释 | ✅ 注明依赖公式绝对下限及未来调整时的复核条件 |

## 仍开放

| 项 | 级别 | 持续轮次 | 说明 |
|---|---|---|---|
| — | — | — | 无开放项。第 12 轮的全部发现（O-10-1~O-10-5 与文档项）已分别在 13/14/15 轮闭环 |

> 说明:台账原停在轮 9(776)。第 10/11 轮(worldgraph/供应商)未登记,补记于第 12 轮;O-10-1~O-10-5 与文档待办已全部于第 13-15 轮修复并锁定(O-8~O-9 及更早问题均已闭环)。

## 已排除的疑似问题(误报)

- **O-7**(第 3 轮误纳,复核后排除):validate.js suspense「resolved 存在但 planted 缺失时绕过因果倒置检查」被误判为缺陷。实为**有意的分层设计**——schema 层只拦结构矛盾(resolved<planted 的绝对矛盾仍被 100-104 拦截,resEarly 测试验证),planted 缺失属「数据不完整」而非「结构非法」,交给规则层检出:rules.js:203-206 `suspense/unplanted` 报 error 并提示补 planted。三重证据:① run-tests.js:155 显式断言「无 planted 时 resolved 不误拒」;② 场景 6 + suspense-bug-canon.json(`{id:"s1",resolved_in_chapter:2}`)通过 schema 但 compile 检出 unplanted 且 exit 1(若 schema 拒绝则 exit 2,场景 6 失效);③ null 表示「未回收」语义一致(validate.js:88、rules.js:203)。若按 O-7 建议 schema 直接报错,会破坏 155 断言与 unplanted 规则机制,并使 LLM 提取的 canon 因漏填 planted 整体校验失败——不应修复。
- suppress.js MULTILINE_RE 与 extract.js normalizeSuppressBlocks"口径不一致":实际是 rawLines(供 multiline 检测)与归一化行(正文统计)的有意分工,compile.js:99-100 注释明确——非缺陷。
- incremental.js timeline `number=null` 去重:validate.js:113 强制 timeline.chapter 非负整数,不会为 null——非缺陷。
- extraction.js timeline evidence key 类型不一致:JS 对象 key 恒为字符串,obj[1]===obj['1']——非缺陷。
- CLI 输入路径无 isInsideRoot:CLI 信任本机用户,isInsideRoot 是 IDE 端点(请求体来源外部)防护,memory 无 CLI 路径校验约束——纵深防御观察项,非缺陷。

## 结构性观察

- 回滚协议已统一:新回滚点应复用 rollbackFile + heldLockTargets,勿手写 copyFileSync。
- 锁目标过滤(fs.existsSync(dirname))意味着"写失败"场景天然不持该文件锁——落盘失败回滚依赖 rollbackFile 的临时取锁/在握判断,协议自洽。
- chatTextValidated 对校验失败会重试:fake LLM 服务器拿锁必须 acquire-once(场景 49l 锁冲突部分教训,run-tests.js:2880-2883;该部分内部变量沿用开发期旧编号 49m,勿与第 2 轮新增回归场景 49m 混淆)。
- README 断言数快照由测试末尾自动校验,改测试必须同步改 README(run-tests.js 末尾)。
